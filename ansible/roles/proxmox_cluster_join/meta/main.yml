---
galaxy_info:
  role_name: proxmox_cluster_join
  author: murrou
  description: Join a Proxmox VE node to an existing cluster via the REST API (idempotent — skips if node already in cluster)
  min_ansible_version: "2.10"
  platforms:
    - name: Debian
      versions:
        - trixie

dependencies: []
